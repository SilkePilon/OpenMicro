/target

# SDD scratch (briefs, reports, review packages, ledger)
.superpowers/

# Claude Code scratch (worktrees, session state)
/.claude/

# The firmware is a separate workspace, so /target does not cover it.
firmware/target/
