[workspace]
resolver = "2"
members = [
    "crates/openmicro-proto",
    "crates/openmicro-effects",
    "crates/openmicrod",
    "crates/openmicro",
    "crates/openmicro-hook",
]

[workspace.package]
edition = "2021"
license = "MIT"

[workspace.dependencies]
serde = { version = "1", default-features = false, features = ["derive"] }
postcard = "1"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net", "io-util", "sync", "time", "fs", "signal"] }
serde_json = "1"
toml = "0.8"
anyhow = "1"
clap = { version = "4", features = ["derive"] }
ratatui = "0.28"
crossterm = "0.28"
async-trait = "0.1"
bluer = { version = "0.17", features = ["bluetoothd"] }
uuid = "1"
futures = "0.3"
hidapi = "2.6"
# Raw-mode termios for the cable link. A tty in its default line discipline
# rewrites binary (LF -> CRLF, flow control on 0x11/0x13), which silently
# corrupts every framed LED packet.
libc = "0.2"
